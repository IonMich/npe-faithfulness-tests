# Result Artifact Index

Canonical result artifacts now live inside each run folder under `runs/<category>/<run>/results/`.

Start with `runs/README.md` for the full run index, or `runs/00_successful_runs/README.md` for the successful/reference subset.
